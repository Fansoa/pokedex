/**
 * A no-operation (noop) function that does nothing and returns null.
 * Useful as a placeholder or default callback.
 * @returns {null} Always returns null.
 */

const noop = () => null;

export default noop;
