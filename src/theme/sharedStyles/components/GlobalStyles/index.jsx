import SHARED_STYLES from "@/src/theme/sharedStyles/constants/SHARED_STYLES";
import { Global } from "@emotion/react";

const GlobalStyles = () => <Global styles={SHARED_STYLES} />;

export default GlobalStyles;
